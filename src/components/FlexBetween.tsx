import { Box, styled } from "@mui/material";


//syntax for styling the basic componenets 
const FlexBetween = styled(Box)({
    display:"flex",
    justifyContent:"space-between",
    alignItems:"center",
})

export default FlexBetween